var Config = Config || {};

// Custom nickname colors in chat.
// Can be { color: "css color value" } 
// or a nickname that hashes to the same color, by simply putting the nick
Config.customcolors = {
    'tustin2121' : 'tustin2121',
    'megacharizard' : { color : "#FF0000" },
    'abyll' : { color : "#007FFF" },
};